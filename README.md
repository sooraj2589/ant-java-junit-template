Ant-Java-JUnit build template
=============================

A template for Ant builds for Java + JUnit projects.

### Making this thing work

Just run `ant`. The default target is `test`; to run another target, specify it typing `ant <other-target>`.

